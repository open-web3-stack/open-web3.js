{{> header }}

{{{ imports }}}

export interface StorageType extends BaseStorageType {
{{#each modules}}
  {{{name}}}: {
  {{~#each items}}
    {{> docs}}
    {{{name}}}: {{{entryType}}};
  {{/each}}
  };
{{/each}}
}
{{> footer }}